# daily-time-narrative-redesign - Work Plan

> Execution ownership transferred on 2026-08-06 to `.omo/plans/dayline-five-day-density-timer-truth.md` by `.omo/start-work/dayline-successor-transfer.md`. Tasks 1–7 retain their historical receipts; Task 8 and F1–F4 remain incomplete and must not be resumed or marked complete here.

## TL;DR (For humans)

**What you'll get:** 회색 체크보드를 `오늘의 흐름` 타임라인으로 바꿉니다. 접힌 화면에서는 지금을 중심으로 한 8시간, 펼친 화면에서는 하루 전체를 세 구간으로 보여 주며, 타이머 기록·카테고리 색·오늘 합계가 같은 데이터에서 동시에 갱신됩니다.

**Why this approach:** 기록은 이미 정확한 시작·종료 시각으로 저장되므로 15분 칸으로 다시 뭉개지 않습니다. 한 번 읽은 오늘 기록을 타임라인과 요약이 함께 사용해 표시 불일치와 반복 조회도 없앱니다.

**What it will NOT do:** 과거 날짜 탐색, 기록 편집, 캘린더 연동, 자동 활동 추적, 목표·스트릭·분석 대시보드는 추가하지 않습니다. 기존 타이머·할 일·노치 동작과 일일 로그 형식도 바꾸지 않습니다.

**Effort:** Large
**Risk:** High - 시간대/DST·겹친 기록·실시간 갱신과 좁은 macOS 노치 화면을 동시에 정확하게 검증해야 합니다.
**Decisions I made for you:** 카테고리는 색과 함께 반복 가능한 선 패턴을 사용하고, Escape는 기존처럼 패널 닫기만 담당합니다. 조회 실패 시 마지막 정상 화면을 유지하며, 커밋 이력이 없는 작업공간은 파일별 해시로 보호합니다.

Your next move: 이 계획은 승인된 요청에 따라 고정밀 검토를 통과한 뒤 자동 실행됩니다. Full execution detail follows below.

---

> TL;DR (machine): Large/high-risk replacement of the current checkerboard with a shared-snapshot, exact-session Dayline; eight execution tasks plus four independent final gates.

## Scope

### Must have

- Replace the production checkerboard with `오늘의 흐름`: one rolling compact lane and three expanded lanes (`00–08`, `08–16`, `16–24`).
- Render exact `SessionEntity` boundaries clipped to the current local day; never infer UI boundaries from 15-minute slots.
- Use one `DayActivityStore` owned by `MainPanelView`; one Core Data fetch per reload feeds the Dayline and `TodayUsageSummaryView`.
- Reproject copied immutable records on one-second ticks without refetching; reload on relevant session/category saves and local-day rollover.
- Deterministically normalize overlaps for display and totals without mutating stored sessions or double-counting time.
- Keep active or most-recent detail visible without interaction; hover, focus, and selection only refine the detail.
- Pair category color with a stable non-color stroke pattern and complete visible/accessibility text.
- Move the labelled expand/collapse control into the Dayline card header while retaining the existing date header.
- Preserve timer/category continuity, tasks, settings, notch shell/hover behavior, collapsed progress ring, and daily-log compatibility.
- Pass SwiftPM tests/build, a fresh Xcode app build, exact-bundle native interaction QA, accessibility/reduced-motion checks, dual visual review, and final independent audits.

### Must NOT have (guardrails, anti-slop, scope boundaries)

- No history/day navigation, calendar import, automatic app tracking, goals, streaks, coaching, billing, analytics dashboard, or record editing.
- No Core Data migration, new dependency/chart library, preview-only fixture in production, horizontal scrolling, decorative pulse, hover-only meaning, or color-only distinction.
- Do not delete or change the contract of `HeatmapSlot`, `SessionEntity.sessions(forSlot:)`, `DailyLogWriter`, `FloatingPanelController.flushDailyLog()`, `AppDelegate`, or `MultiDayHeatmapView`; they remain logging or compile-time consumers.
- Do not redesign tasks, timer inputs, category editing, notch geometry, settings, or the collapsed timer ring.
- Do not rename/delete Swift source files or regenerate/hand-edit `NativeScheduler.xcodeproj`; adapt existing compile-registered files to avoid project churn.
- No broad formatting, cleanup, staging, commit, deletion, or writes outside each task's allowlist. The parent repository is unborn and all project paths are untracked user data.

## Verification strategy

> Zero human intervention: implementation, tests, native UI interaction, screenshots, accessibility inspection, cleanup, and review are agent-executed.

- Test decision: TDD with XCTest for snapshot/overlap/calendar/layout/semantics boundaries; characterized visual RED plus compile/runtime verification for SwiftUI composition.
- Baseline gate: Todo 1 is the serial prerequisite for every editing task. Before any mutation it preflights every required native QA permission without requesting a TCC grant, records the recursive baseline manifest, verifies `swift build` and all 35 existing tests actually execute and pass, and captures the old checkerboard from a PID-bound app bundle.
- Build gates: Todos 2–5 run the targeted/full SwiftPM commands named in their own acceptance sections. Integration Todo 6 and frozen-source Todo 7 each independently pass the full SwiftPM tests/build and a fresh Xcode build; neither toolchain substitutes for the other. Todo 8 and F3/F4 build fresh Xcode bundles at the accepted Todo 7 source digest and require Todo 7's SwiftPM result for that exact digest. Every command writes only below the active attempt root and its task cleans that generated directory.
- Source identity: every command runs with canonical working directory `/Users/chan/Projects/native_schedular`. Because `HEAD` does not exist, Todo 1 records a sorted recursive SHA-256 manifest of that complete tree, including packages, project configuration, resources, source, tests, and design files. Exclude only `.omo/evidence/**`, `.omo/start-work/**`, `.omo/boulder.json`, `.omo/plans/daily-time-narrative-redesign.md`, `.omo/drafts/daily-time-narrative-redesign.md`, `NativeScheduler/.build/**`, `NativeScheduler/.swiftpm/**`, task-local `DerivedData*/**`, and `.DS_Store`. Before the first edit it also creates the canonical absolute `PRISTINE_ROOT=/Users/chan/Projects/native_schedular/.omo/evidence/start-work/daily-time-narrative-redesign/pristine` containing a byte-complete relative-path archive of that same scope, the manifest, archive SHA-256, and mode metadata, then makes the capsule read-only. Reviewers bind to the manifest digest and permit final differences only at task allowlists; every other add/change/delete fails.
- Evidence root: before dispatch, the orchestrator resolves `ATTEMPT_ROOT` to the canonical absolute path of the first unused literal `/Users/chan/Projects/native_schedular/.omo/evidence/start-work/daily-time-narrative-redesign/aN`, rejects symlinks/non-directories, and records it in Boulder/ledger. Every task and final lane in that run uses only that one root; cross-attempt evidence is forbidden.
- Failure policy: any rejected task/final lane starts a fresh attempt and reruns Todos 1–8 plus F1–F4. Before each atomic source replacement, an editing executor first writes the complete intended payload into `$ATTEMPT_ROOT/task-N-staging/<operation-id>`, fsyncs that regular file and its parent, then appends and fsyncs a hash-chained `begin` row in `$ATTEMPT_ROOT/task-N-write-journal.jsonl` containing operation ID, relative target, verified pre-hash, staged path, intended post-hash, and prior-row hash. It atomically replaces the target from that staged payload, fsyncs target and parent, then appends/fsyncs a matching `complete` row with observed post-hash. On recovery, an unmatched `begin` is reconciled only when current content equals the verified pre-hash (not applied) or intended post-hash (applied); any other value blocks without overwrite. Completed or reconciled journal hashes, then the last completed owner hash, then pristine are the only expected-current states. Leftover staging payloads are retained/marked stale unless their hash and journal operation match; they are never promoted by name alone. The pristine capsule is likewise built under a unique partial directory, fsynced with a completion marker/archive hash/mode manifest, and atomically promoted only when complete; an interrupted partial capsule is never accepted or used for restoration. Before Todo 1 of `aN` where N>1, restoration verifies the completed pristine capsule, reconciles journals, requires every planned mutable path to match its permitted state and every non-allowlisted path to match pristine, then atomically restores the nine planned mutable files. Any unexplained hash blocks. Prior tests/screenshots/approvals never satisfy new checkboxes.
- Runtime resource policy: use task-local DerivedData/helper binaries. Every native app/AX task owns `$ATTEMPT_ROOT/native-qa.lock` in its allowlist and atomically acquires it; no two native QA lanes overlap. Before acquisition, stale-lease recovery verifies the recorded PID, exact executable/payload, QA home, and prior production-state hashes. It kills only a still-running exact recorded process, restores an accessibility setting only if its current value equals the journaled task-set value (external drift blocks), verifies production state, and then retires the stale lease. Record and guard Full Keyboard Access, VoiceOver, and Reduce Motion before every change.
- Isolated exact-bundle launch: resolve the exact bundle/executable as `APP_PATH`/`APP_EXEC`, create canonical task-local `QA_HOME`, and preflight with an inline probe that `FileManager` Application Support, Core Data, daily-log paths, and `UserDefaults` resolve under it. Hash production SQLite/WAL/SHM, daily logs, preferences, and login-item status, then launch the exact executable directly as `env HOME="$QA_HOME" CFFIXED_USER_HOME="$QA_HOME" "$APP_EXEC" -didRegisterLaunchAtLogin YES`. Verify environment, arguments, PID, payload, and that every created database/log/preference file stays below `QA_HOME`; the volatile argument bypasses login-item registration. Except for the final inspected app, terminate only the owned PID with `kill -KILL "$PID"`, then require production state unchanged. The final F4 app may remain running collapsed only inside its isolated `QA_HOME`, with production identity unchanged and PID/payload recorded.

## Execution strategy

### Parallel execution waves

- Wave 1: Todo 1 runs alone so permission preflight and the complete-tree baseline precede every edit.
- Waves 2–6: Todos 2, 3, 4, 5, and 6 run serially in numeric order. This unborn shared tree cannot isolate concurrent builds from a peer's source mutation.
- Wave 7: Todo 7 runs the frozen-source automated build/test gate.
- Wave 8: Todo 8 starts only after Todo 7, then acquires the native-QA lease for the exact app/host gate.
- Final verification phase: F1 and F2 may run in parallel on frozen source. F3 obtains/releases the native-QA lease; F4 begins only after F3 releases it and independently builds its own bundle/host. All four approve the same source-manifest digest.
- Critical path: 1 → 2 → 3 → 4 → 5 → 6 → 7 → 8 → F3 → F4. No live writer or build overlaps another writer/build.

### Dependency matrix

| Todo | Depends on | Blocks | Can parallelize with |
| --- | --- | --- | --- |
| 1 | none | 2 | none |
| 2 | 1 | 3 | none |
| 3 | 2 | 4 | none |
| 4 | 3 | 5 | none |
| 5 | 4 | 6 | none |
| 6 | 5 | 7, 8 | none |
| 7 | 6 | 8 | none |
| 8 | 7 | F1–F4 | none |

## Todos

- [x] 1. Lock the Dayline design contract and capture the old-board baseline
  - Outcome: `DESIGN.md` and the frontend state describe the approved Dayline states, accessibility, motion, and exclusions; exact old-board evidence proves the visual RED.
  - Owned allowlist: `DESIGN.md`; `.omo/frontend-design/state.md`; the retry-restore-only paths `NativeScheduler/Sources/NativeScheduler/Features/Heatmap/HeatmapViewModel.swift`, `NativeScheduler/Tests/NativeSchedulerTests/HeatmapLiveTests.swift`, `NativeScheduler/Sources/NativeScheduler/Features/Heatmap/HeatmapView.swift`, `NativeScheduler/Tests/NativeSchedulerTests/HeatmapSlotTests.swift`, `NativeScheduler/Sources/NativeScheduler/Shared/DesignSystem/Colors.swift`, `NativeScheduler/Sources/NativeScheduler/Features/Heatmap/TodayUsageSummaryView.swift`, `NativeScheduler/Sources/NativeScheduler/Features/FloatingPanel/MainPanelView.swift`; `PRISTINE_ROOT/**`; `$ATTEMPT_ROOT/task-1-*`; `$ATTEMPT_ROOT/native-qa.lock` only. On `a1`, only the two design docs may change; on retry, the seven product/test paths may only be restored byte-for-byte before the baseline.
  - Parallelization: Wave 1 | Blocked by: none | Blocks: 2.
  - Baseline first: on `a1`, record the recursive manifest, each planned file's pristine hash, and create/read-protect the byte-complete capsule before any edit. On retry, perform stale native-QA/process/settings/lease recovery, reconcile write journals, build the expected-current hash map exactly as Failure policy specifies, perform guarded restoration, and record every precondition/restored hash; never overwrite a mismatch. Then preflight native capabilities/settings without requesting grants; run `swift build --package-path NativeScheduler --scratch-path "$ATTEMPT_ROOT/task-1-scratch"`, run `swift test --package-path NativeScheduler --scratch-path "$ATTEMPT_ROOT/task-1-scratch"` and require exactly the 35 inventoried baseline tests with 0 failures, and run `xcodebuild -project NativeScheduler/NativeScheduler.xcodeproj -scheme NativeScheduler -configuration Debug -derivedDataPath "$ATTEMPT_ROOT/task-1-DerivedData" build`; bind PID/window/payload from the task-local isolated launch, recapture the old checkerboard, and record the old contract search as RED.
  - Implement: authorize `오늘의 흐름`, one rolling compact lane, three fixed expanded lanes, visible active/recent detail, stable color-plus-pattern category tokens, labelled in-card toggle, preserved date header, focus/pointer/accessibility actions, Reduce Motion, measured runtime sizing, and explicit current-day-only exclusions. Document retained legacy 15-minute/logging APIs as compatibility infrastructure, not UI truth.
  - Must not: edit Swift/product code, change shell geometry/palette outside named Dayline tokens, or cite the stale historical screenshot as current runtime proof.
  - Acceptance: pristine archive/manifest hashes verify and the pre-edit tree exactly matches pristine; retry restoration and journal-reconciliation logs prove current-hash ownership before each replacement; task-local SwiftPM build, the exact 35-test baseline run, and fresh Xcode build all exit 0 with command/exit/test inventory in `$ATTEMPT_ROOT/task-1-swift-build.txt`, `$ATTEMPT_ROOT/task-1-baseline-tests.txt`, and `$ATTEMPT_ROOT/task-1-xcode-build.txt`. Post-edit contract contains `오늘의 흐름`, `00–08`, `08–16`, `16–24`, `active or most recent`, `Reduce Motion`, non-color pattern semantics, and scope guardrails; post hashes differ from pristine only for the two owned docs.
  - QA happy: under the native-QA lease, a `lazycodex-qa-executor` records production hashes, proves the task-local `QA_HOME` routing probe, launches the exact old executable with the isolated environment/volatile argument, verifies environment and process arguments plus `qa_helper.swift windows PID`, and captures the owned checkerboard showing the new header/detail/control are absent.
  - QA failure: reject wrong-owner, occluded, empty, predated, or source-digest-mismatched images and any unexpected product-file hash change.
  - Evidence: `PRISTINE_ROOT/source.tar`, `PRISTINE_ROOT/manifest.txt`, `PRISTINE_ROOT/archive-sha256.txt`, `PRISTINE_ROOT/complete`, `$ATTEMPT_ROOT/task-1-restoration.txt`, `$ATTEMPT_ROOT/task-1-write-journal.jsonl`, `$ATTEMPT_ROOT/task-1-native-recovery.txt`, `$ATTEMPT_ROOT/task-1-native-preflight.txt`, `$ATTEMPT_ROOT/task-1-qa-home-routing.txt`, `$ATTEMPT_ROOT/task-1-settings-before-after.txt`, `$ATTEMPT_ROOT/task-1-project-baseline-manifest.txt`, `$ATTEMPT_ROOT/task-1-project-baseline-digest.txt`, `$ATTEMPT_ROOT/task-1-swift-build.txt`, `$ATTEMPT_ROOT/task-1-baseline-tests.txt`, `$ATTEMPT_ROOT/task-1-xcode-build.txt`, `$ATTEMPT_ROOT/task-1-pre-post-manifest.txt`, `$ATTEMPT_ROOT/task-1-contract-red.txt`, `$ATTEMPT_ROOT/task-1-old-compact.png`, `$ATTEMPT_ROOT/task-1-old-expanded.png`, `$ATTEMPT_ROOT/task-1-owner.txt`.
  - Cleanup: terminate only the launched old-bundle PID with `kill -KILL "$PID"`; require persistence/preferences/login-item and accessibility settings to equal preflight; remove task-local scratch/DerivedData/helper; release native-QA lease; retain evidence.
  - Adversarial: `dirty_worktree`, `stale_state`, `misleading_success_output`, wrong-owner window.
  - Commit: N — repository has no `HEAD`; do not stage or commit.

- [x] 2. Build the immutable single-fetch daily snapshot and live store in the existing model file
  - Outcome: one main-actor `DayActivityStore` fetches/copies today once per reload, derives normalized segments/totals/fallback selection, and reprojects cached records on ticks.
  - Owned allowlist: `NativeScheduler/Sources/NativeScheduler/Features/Heatmap/HeatmapViewModel.swift`; `NativeScheduler/Tests/NativeSchedulerTests/HeatmapLiveTests.swift`; `$ATTEMPT_ROOT/task-2-*` only.
  - Parallelization: Wave 2 | Blocked by: 1 | Blocks: 3 | Parallel with: none.
  - References: `ManagedObjects.swift:34-64` exact session/category fields; `HeatmapViewModel.swift:8-184` repeated fetch/observer seam; `TimerViewModel.swift:82-107,135-149,184-195` session lifecycle; `HeatmapLiveTests.swift:10-129` in-memory clock/calendar conventions.
  - Baseline first: verify Todo 1 manifest; hash owned files; run `swift test --package-path NativeScheduler --scratch-path "$ATTEMPT_ROOT/task-2-scratch" --filter HeatmapLiveTests` and the full 35-test suite unchanged. Add failing tests before implementation and preserve the RED output with actual failed assertions or missing symbols.
  - Value boundary: add immutable Swift value records in the existing compile-registered file containing session UUID, start, optional end, category UUID/name/resolved hex, and running state. Map managed objects to these values inside the context fetch; published snapshots/views never retain `NSManagedObject`.
  - Fetch contract: issue exactly one request per reload with predicate `startTime < nextStartOfDay AND (endTime == nil OR endTime > startOfDay)`. Initial load, each relevant inserted/updated/deleted `SessionEntity` or `CategoryEntity` context save, and local-day rollover each cause one reload. Ordinary one-second ticks only reproject cached records. Unrelated saves do nothing. A same-day fetch failure preserves the last good same-day snapshot and sets a testable non-disruptive error flag; a rollover fetch failure publishes an empty snapshot for the new day plus the error, never yesterday's data. A later success clears it.
  - Normalization: all ranges are half-open `[start,end)`. Clip starts to day start and ends to `min(end ?? now, now, nextStartOfDay)`; ignore starts at/after `now`, zero/negative ranges, and stored `end < start`; partition at every valid boundary; latest `startTime` wins; equal starts choose the lexicographically greatest lowercase UUID string; merge adjacent winning intervals only when session/category identity matches. Totals sum winning intervals only, so total never exceeds elapsed day duration. Every resulting interval has a stable unique `SegmentID` composed of its source session UUID plus normalized winning-piece start instant. Thus nonadjacent pieces split from one session are distinct, while an open piece keeps the same identity as only its end advances on ticks; a new overlap that changes the winning-piece start intentionally creates a new identity.
  - Snapshot: expose chronological segments, category totals, total tracked seconds, active-or-most-recent `SegmentID`, immutable `asOf`, `dayStart`, `nextDayStart`, `calendarIdentifier`, `localeIdentifier`, and `timeZoneIdentifier`. Active is the winning segment derived from an originally open record whose clipped half-open end equals `asOf`; otherwise choose the segment with greatest end not after `asOf`, breaking ties by the same UUID rule. Every tick/reload replaces these projection fields together with the segments.
  - Legacy boundary: retain `HeatmapViewModel`, `HeatmapViewModel.SlotInfo`, `HeatmapSlot`, and existing APIs unchanged for `MultiDayHeatmapView`/logging compile compatibility; production ownership changes only in Todo 6.
  - Acceptance: focused tests prove one initial fetch, zero tick refetches, exactly one relevant-save refetch, zero unrelated-save refetches, one rollover refetch, last-good-on-error/recovery, immutable copying, day clipping, open growth, invalid/future filtering, nested/partial/equal-start overlaps, adjacent merge, no double count, category-total equality, and active/recent fallback. A later-starting overlap must split one source session into two nonadjacent winning segments with distinct stable `SegmentID`s and no duplicate collection identity; repeated live ticks that only advance an open endpoint must preserve its ID, pinned selection, focus model, and collection identity. An in-memory context integration test inserts/saves an open `SessionEntity`, closes it, then saves a replacement-category session exactly as `TimerViewModel` does and verifies one store publication updates segment endpoint/color/total at each step. Then the full suite executes at least the 35 baseline tests plus new cases with 0 failures.
  - QA happy: XCTest uses an in-memory store, throwing fetch spy, fixed clock, and fixed calendars; compare segment-duration sum, per-category sum, published total, and the simulated timer-save sequence exactly.
  - QA failure: spring-forward/fall-back, cross-midnight, save storm, equal start, fetch error, nil category, malformed hex, and stale previous-day cache must produce the locked results rather than crashes, duplicate time, or refetch-on-tick.
  - Evidence: `$ATTEMPT_ROOT/task-2-red.txt`, `$ATTEMPT_ROOT/task-2-focused-green.txt`, `$ATTEMPT_ROOT/task-2-fetch-counts.txt`, `$ATTEMPT_ROOT/task-2-full-tests.txt`, `$ATTEMPT_ROOT/task-2-pre-post-manifest.txt`.
  - Cleanup: destroy in-memory stores/observers and `$ATTEMPT_ROOT/task-2-scratch`; no app process or preview fixture remains.
  - Adversarial: `flaky_tests` via fixed clock/calendar, `misleading_success_output` via executed-test inventory, save storms, stale cache.
  - Commit: N — repository has no `HEAD`; do not stage or commit.

- [x] 3. Define deterministic Dayline lane geometry, strings, and non-color tokens
  - Outcome: pure, testable helpers in the existing view file resolve compact/expanded ranges, block fractions, visible/spoken strings, and stable pattern tokens without SwiftUI runtime dependence.
  - Owned allowlist: `NativeScheduler/Sources/NativeScheduler/Features/Heatmap/HeatmapView.swift`; `NativeScheduler/Tests/NativeSchedulerTests/HeatmapSlotTests.swift`; `NativeScheduler/Sources/NativeScheduler/Shared/DesignSystem/Colors.swift`; `$ATTEMPT_ROOT/task-3-*` only.
  - Parallelization: Wave 3 | Blocked by: 2 | Blocks: 4 | Parallel with: none.
  - References: `HeatmapView.swift:18-31,33-69,96-153`; `ManagedObjects.swift:155-185`; `Colors.swift:4-23`; `HeatmapSlotTests.swift:5-45`.
  - Baseline first: verify Todo 1 manifest; hash files; run existing `HeatmapSlotTests` and full suite with `--scratch-path "$ATTEMPT_ROOT/task-3-scratch"`; add layout/semantic tests first and preserve failing RED output.
  - Projection inputs: geometry/formatting accept only snapshot `asOf`, bounds, calendar/locale/time-zone identifiers; production rendering contains no direct `Date()` or `.current` read. Reconstruct the calendar with the snapshot time zone so the fixed host and tests are deterministic.
  - Expanded geometry: local wall-clock anchors are calendar-derived day start, 08:00, 16:00, and following start of day. Each lane maps positions by elapsed seconds between its actual anchors. Spring-forward invents no missing hour; fall-back keeps repeated time chronological; the three lane durations sum to the calendar day's actual 23/24/25 hours.
  - Compact geometry: candidate start is the calendar start of the hour two calendar hours before `now`; form an eight-elapsed-hour interval, clamp to the local day, and shift only enough to retain eight elapsed hours when the day has capacity. On an ordinary day tests lock 00:30→00:00–08:00, 12:30→10:00–18:00, and 23:30→16:00–24:00, with two-hour tick labels and now fractions.
  - Segment geometry: clip to each lane, clamp fractions to 0...1, discard zero width, and do not impose a misleading minimum duration width. Short blocks remain visible through a 1px minimum stroke only, while the fill width stays proportional.
  - Non-color token: derive a stable per-category stroke dash/cap pattern from the category UUID (nil/default has its own pattern), render it on every block including short blocks, and repeat the same swatch before visible detail/summary text. Color remains resolved through the existing shared category resolver.
  - Semantics: exact shipped title `오늘의 흐름`; accessibility header `오늘의 흐름, 오늘 기록 {duration}`; each segment label contains category or `Default`, localized start/end, duration, and active state. When a fall-back transition repeats the same wall-clock label, include the localized time-zone abbreviation/offset in both visible detail and spoken range so the first and second occurrence are unambiguous. Detail formatting handles under-one-minute and long/CJK names without requiring inline block text.
  - Acceptance: tests cover the three compact examples, all expanded anchors, 23/24/25-hour day sums in named time zones, block clipping/fractions, zero/one-second sessions, 59/60-second formatting, nil/default category, malformed color, deterministic patterns, long CJK names, and complete accessibility strings. Existing 96-slot compatibility tests remain green.
  - QA happy: focused XCTest filters execute nonzero geometry/semantics cases and full suite remains green.
  - QA failure: fixed `24*3600`, NaN/out-of-range fractions, lost repeated-hour ordering, color-only token output, or absent category/range/duration in labels fails assertions.
  - Evidence: `$ATTEMPT_ROOT/task-3-red.txt`, `$ATTEMPT_ROOT/task-3-focused-green.txt`, `$ATTEMPT_ROOT/task-3-dst.txt`, `$ATTEMPT_ROOT/task-3-full-tests.txt`, `$ATTEMPT_ROOT/task-3-pre-post-manifest.txt`.
  - Cleanup: remove task scratch and temporary timezone/log output only; no system timezone change.
  - Adversarial: DST transitions, midnight, long labels, malformed colors, `flaky_tests` from ambient locale/timezone.
  - Commit: N — repository has no `HEAD`; do not stage or commit.

- [x] 4. Render compact and expanded `DaylineView` in the existing board file
  - Outcome: `HeatmapView.swift` contains the production-ready `DaylineView` while retaining legacy `HeatmapView` only for compile compatibility until integration proves no runtime call sites.
  - Owned allowlist: `NativeScheduler/Sources/NativeScheduler/Features/Heatmap/HeatmapView.swift`; `NativeScheduler/Tests/NativeSchedulerTests/HeatmapSlotTests.swift`; `$ATTEMPT_ROOT/task-4-*` only.
  - Parallelization: Wave 4 | Blocked by: 3 | Blocks: 5 | Parallel with: none.
  - Baseline/RED: re-hash inherited Todo 3 outputs and reject drift; use Todo 1 owned baseline screenshots as visual RED. Add view-state contract tests in the existing test file before rendering and preserve their failure.
  - Render: `DaylineView(snapshot:isExpanded:)` derives now-marker, lane bounds, labels, and rollover state exclusively from the snapshot projection fields, never ambient clock/calendar. The header shows `오늘의 흐름`, formatted total, and native `rectangle.expand.vertical`/`rectangle.compress.vertical` button with visible tooltip and accessibility labels `하루 전체 보기`/`주변 시간만 보기`. Compact renders one range with two-hour ticks; expanded renders three labelled lanes; both render elapsed-untracked, future, patterned category segments, selected/active state, and a single now marker.
  - Meaning/interaction: active segment is default, otherwise most recent. Hover/focus changes detail transiently; click or Enter/Space pins a `SegmentID`; pointer/focus departure restores fallback unless pinned; an accessibility action `현재 활동으로 돌아가기` clears the pin. Snapshot changes clear only a selected `SegmentID` that is no longer present. Rendering/selection uses `SegmentID`, never the source session UUID alone, so split-session pieces remain distinct in `ForEach` and interaction. Do not add an Escape command because `NotchWindow` exclusively owns Escape-to-collapse.
  - Accessibility: segment controls are keyboard focusable, use the complete semantic labels from Todo 3, expose selected/active values/actions, preserve AX traversal order header→toggle→chronological blocks and keyboard Tab order toggle→chronological blocks, hide decorative shapes, and keep visible focus. Short blocks omit cramped inline text but retain patterned stroke and accessibility element.
  - Motion/sizing: use native SwiftUI primitives, no Canvas/chart dependency; only category/selection crossfade and live endpoint change may animate. When `accessibilityReduceMotion` is true, use no nonessential transition. Use flexible measured width with 11pt-or-larger labels, no horizontal scroll, and no hard-coded stale 520pt assumption.
  - Acceptance: focused tests using `$ATTEMPT_ROOT/task-4-scratch` prove view-state fallback/pin reset, split-session `ForEach` identity/selection uniqueness, live-tick retention of pin/focus while only an open endpoint advances, chronological keyboard/AX models, control strings, reduced-motion transaction selection, and empty/active/recent behavior; `swift build --package-path NativeScheduler --scratch-path "$ATTEMPT_ROOT/task-4-scratch"` passes; pre/post manifest contains only owned files.
  - QA happy: a temporary task-local SwiftUI host or Todo 6 integrated surface renders empty, compact live, and expanded live states; no production preview fixture ships.
  - QA failure: very short sessions, many adjacent segments, default category, long CJK category, reduced motion, future region, near-midnight clamp, pointer leave, and disappearing selected session do not clip, trap focus, or lose fallback detail.
  - Evidence: `$ATTEMPT_ROOT/task-4-red.txt`, `$ATTEMPT_ROOT/task-4-focused-green.txt`, `$ATTEMPT_ROOT/task-4-swift-build.txt`, `$ATTEMPT_ROOT/task-4-pre-post-manifest.txt`, and any temporary-host captures under `$ATTEMPT_ROOT/task-4-host-*` clearly labelled non-final.
  - Cleanup: remove task-local host, scratch, DerivedData, and process; no preview/debug fixture remains in source.
  - Adversarial: tiny width, short blocks, selection disappearance, keyboard-only, Reduce Motion, stale inherited source.
  - Commit: N — repository has no `HEAD`; do not stage or commit.

- [x] 5. Make the existing usage summary consume the shared snapshot
  - Outcome: `TodayUsageSummaryView` receives the same immutable snapshot as Dayline and no longer owns a fetcher, context observer, or timer.
  - Owned allowlist: `NativeScheduler/Sources/NativeScheduler/Features/Heatmap/TodayUsageSummaryView.swift`; `NativeScheduler/Tests/NativeSchedulerTests/HeatmapLiveTests.swift`; `$ATTEMPT_ROOT/task-5-*` only.
  - Parallelization: Wave 5 | Blocked by: 4 | Blocks: 6 | Parallel with: none.
  - Baseline first: verify Todo 2 hashes/digest; run the relevant inherited snapshot tests and record the old summary's second observer/96-slot aggregation as RED. Add failing construction/total-consistency tests before changing the view.
  - Implement: remove `TodayUsageSummaryViewModel` and its duplicate slot aggregation, timer, and context-save observer. Accept the shared snapshot/category totals as values. Preserve the ring/list appearance, visible category name/pattern/color/percentage/duration, deterministic sorting, five-entry-plus-`Etc` behavior, and decorative-ring accessibility hiding.
  - Sorting/rounding: order by descending normalized duration, then `String.compare` with case-insensitive options and explicit `Locale(identifier: snapshot.localeIdentifier)`, then category UUID; never use ambient localized comparison. `Etc` sums remaining normalized durations. Percentages may round for display, but durations and Dayline total derive from exact shared seconds.
  - Acceptance: tests under `$ATTEMPT_ROOT/task-5-scratch` prove zero, one, six-plus, equal-duration, nil/default, active-growth, rounding, and explicit Korean/English locale tie cases while ambient process locale differs; identity/equality assertions show Dayline total equals summary category sum; source has no independent observer/timer/fetch; focused/full suites pass.
  - QA happy: construct the summary from fixed snapshots and independently prove visible rows, exact durations, `Etc`, and snapshot-locale ordering. Integrated category-save rendering belongs entirely to Todo 6.
  - QA failure: duplicate duration, 15-minute rounding drift, independent refresh, equal-sort nondeterminism, or `Etc` mismatch fails tests.
  - Evidence: `$ATTEMPT_ROOT/task-5-red.txt`, `$ATTEMPT_ROOT/task-5-focused-green.txt`, `$ATTEMPT_ROOT/task-5-full-tests.txt`, `$ATTEMPT_ROOT/task-5-observer-audit.txt`, `$ATTEMPT_ROOT/task-5-pre-post-manifest.txt`.
  - Cleanup: confirm observer/timer invalidation code is absent and no test process remains.
  - Adversarial: six-plus categories, equal durations, live active session, `misleading_success_output`, stale Todo 2 snapshot.
  - Commit: N — repository has no `HEAD`; do not stage or commit.

- [x] 6. Integrate the one-owner Dayline into `MainPanelView`
  - Outcome: the live panel owns one `DayActivityStore`, passes one snapshot to Dayline and summary, and removes the ambiguous standalone capsule without disturbing neighboring features.
  - Owned allowlist: `NativeScheduler/Sources/NativeScheduler/Features/FloatingPanel/MainPanelView.swift`; `$ATTEMPT_ROOT/task-6-*`; `$ATTEMPT_ROOT/native-qa.lock` only.
  - Parallelization: Wave 6 | Blocked by: 5 | Blocks: 7, 8.
  - Baseline/RED: bind to accepted Todo 4/5 source digest; record production references to `HeatmapView`, duplicate view models, and `heatmapToggle` before edit; build once to prove the inherited state is green. Todo 1 screenshots remain surface RED.
  - Implement: create exactly one `@StateObject DayActivityStore` from the live Core Data context; call `DaylineView(snapshot:isExpanded:)`; feed the same published snapshot to `TodayUsageSummaryView`; remove `HeatmapViewModel`/`TodayUsageSummaryViewModel` ownership and the 16pt capsule column. Keep the existing date header, task/timer sizing, settings reload, hover/options lock, category/timer behavior, and notch geometry unchanged.
  - Refresh wiring: the store owns data invalidation/ticks; `MainPanelView` must not add a second timer/observer or reload on Settings dismissal. A category context save is the sole invalidation for that mutation and fetch-count tests require exactly one reload for one save.
  - Legacy boundary: leave `HeatmapView`, `HeatmapViewModel`, `MultiDayHeatmapView`, `HeatmapSlot`, daily-log code, Xcode project, and package topology intact. Production `MainPanelView` must have no call to legacy heatmap/summary models after integration.
  - Acceptance: source audit proves one store owner and no production checkerboard instantiation/standalone toggle; `swift test --package-path NativeScheduler --scratch-path "$ATTEMPT_ROOT/task-6-scratch"`, `swift build --package-path NativeScheduler --scratch-path "$ATTEMPT_ROOT/task-6-scratch"`, and `xcodebuild -project NativeScheduler/NativeScheduler.xcodeproj -scheme NativeScheduler -configuration Debug -derivedDataPath "$ATTEMPT_ROOT/task-6-DerivedData" build` all exit 0 with expected test counts; only `MainPanelView.swift` changes in the product manifest.
  - QA happy: under the native-QA lease, recover stale owned state, record production hashes, prove task-local `QA_HOME` routing, and launch only the exact executable with the isolated environment/volatile argument; verify exact PID/window/payload/environment. Inspect panel interactions against isolated data. A task-local in-memory context plus integrated Dayline/summary host saves synthetic open, close, and replacement-category records and proves one store publication changes both rendered surfaces, pattern/color, endpoint, and equal totals; no live user-store record is opened or created.
  - QA failure: empty/default/live synthetic states in the host plus pointer exit, option popover, Settings presentation, and compact/expanded transitions in the exact app must not crash, spawn duplicate refreshes, close prematurely, mutate persistence, or alter tasks/timer/notch shell.
  - Evidence: `$ATTEMPT_ROOT/task-6-red-references.txt`, `$ATTEMPT_ROOT/task-6-source-audit.txt`, `$ATTEMPT_ROOT/task-6-tests.txt`, `$ATTEMPT_ROOT/task-6-host-integration.txt`, `$ATTEMPT_ROOT/task-6-swift-build.txt`, `$ATTEMPT_ROOT/task-6-xcode-build.txt`, `$ATTEMPT_ROOT/task-6-owner-actions.txt`, `$ATTEMPT_ROOT/task-6-pre-post-manifest.txt`.
  - Cleanup: use `kill -KILL "$PID"`; require persistence/preferences/login-item identity unchanged; remove task scratch/DerivedData/helpers; release native-QA lease; leave accessibility settings unchanged.
  - Adversarial: `dirty_worktree`, stale bundle, duplicate owner, popover lock regression, SPM-pass/Xcode-fail split, `long_running_command`.
  - Commit: N — repository has no `HEAD`; do not stage or commit.

- [x] 7. Run the full automated regression and source-boundary gate
  - Outcome: an evidence-only verifier proves tests/builds/source identity and adversarial data behavior without editing source.
  - Owned allowlist: `$ATTEMPT_ROOT/task-7-*` only; no product/design/test writes.
  - Parallelization: Wave 7 | Blocked by: 6 | Blocks: 8 | Parallel with: none.
  - Baseline/RED guard: independently reproduce Todo 1's recursive manifest algorithm and compare the current tree to its baseline; allow differences only at the union of Todos 1–6 file allowlists and reject every other add/change/delete, including Package.swift, project, resource, or new-source drift. Also hash the complete named source/test/design allowlist and compare it with Todo 6; reject a missing baseline, empty filters, cached/copied logs, or test output that omits new suite/case names.
  - Verify: inventory tests; run each new snapshot/layout/semantics/summary suite separately; run retained `HeatmapSlotTests` and notch/timer regressions; run full `swift test --package-path NativeScheduler --scratch-path "$ATTEMPT_ROOT/task-7-scratch"` and `swift build --package-path NativeScheduler --scratch-path "$ATTEMPT_ROOT/task-7-scratch"`; run `xcodebuild -project NativeScheduler/NativeScheduler.xcodeproj -scheme NativeScheduler -configuration Debug -derivedDataPath "$ATTEMPT_ROOT/task-7-DerivedData" build`; record commands, exit codes, executed counts, durations, and bundle payload hash.
  - Required data assertions: one-fetch/tick counts, fetch failure recovery, overlap/equal-start determinism, invalid/future filtering, total equality, midnight rollover, explicit 23/25-hour DST cases, compact bounds, pattern/AX strings, legacy slot/log compile compatibility.
  - Acceptance: the independently reproduced recursive diff contains only Todos 1–6 allowlisted paths; every named focused suite executes nonzero cases; the full SwiftPM tests/build and fresh Xcode build exit 0 at one recorded source-manifest digest; the resulting bundle payload hash and complete command/test inventory are present; every required data assertion is covered by a named passing test; no source/design/test file changes during this evidence-only task.
  - QA happy: all named tests truly execute and both toolchains exit 0 at the same source-manifest digest.
  - QA failure: repeat suspected flaky cases at least three times with fixed clock/calendar; any intermittent outcome is FAIL, not a retry-to-green. If a command exceeds its recorded normal bound, use one long wait/log completion signal and classify `long_running_command`; do not poll misleading partial output.
  - Evidence: `$ATTEMPT_ROOT/task-7-recursive-manifest-diff.txt`, `$ATTEMPT_ROOT/task-7-source-manifest.txt`, `$ATTEMPT_ROOT/task-7-test-inventory.txt`, `$ATTEMPT_ROOT/task-7-focused-tests.txt`, `$ATTEMPT_ROOT/task-7-full-tests.txt`, `$ATTEMPT_ROOT/task-7-swift-build.txt`, `$ATTEMPT_ROOT/task-7-xcode-build.txt`, `$ATTEMPT_ROOT/task-7-flake-check.txt`, `$ATTEMPT_ROOT/task-7-bundle-provenance.txt`.
  - Cleanup: remove task-7 scratch/DerivedData and processes; retain immutable logs.
  - Adversarial: `flaky_tests`, `misleading_success_output`, `stale_state`, SPM/Xcode divergence, `long_running_command`.
  - Commit: N — evidence only.

- [ ] 8. Execute exact-bundle native macOS visual, interaction, and accessibility QA
  - Outcome: a QA executor observes the requested behavior on the real app, with fresh images/video/AX evidence bound to the same source digest as Todo 7.
  - Owned allowlist: `$ATTEMPT_ROOT/task-8-*`; `$ATTEMPT_ROOT/native-qa.lock` only; no product/design/test writes.
  - Parallelization: Wave 8 | Blocked by: 7 | Blocks: F1–F4 | Parallel with: none.
  - Baseline/RED: before building or changing any setting, repeat Todo 1's exact QA/capture/AX capability and TCC preflight; record Accessibility, Screen Recording, Automation, Full Keyboard Access, VoiceOver, and Reduce Motion permission/state values. Do not request/automate TCC grants. Feed a deliberately mismatched source/payload digest into the provenance gate and require rejection as the stale/wrong-bundle negative control. If any required capture/interaction/AX path lacks permission, stop BLOCKED before scenarios.
  - Provenance first: recover stale owned native state, acquire the native-QA lease, run `xcodebuild -project NativeScheduler/NativeScheduler.xcodeproj -scheme NativeScheduler -configuration Debug -derivedDataPath "$ATTEMPT_ROOT/task-8-DerivedData" build`; hash launcher/debug payload plus production persistence/preferences/login-item state; prove canonical task-local `QA_HOME` routing, then launch only the exact executable with `HOME`/`CFFIXED_USER_HOME` set to that root and the volatile login-item argument. Verify environment/argument and record PID, executable path, loaded payload, window ID/owner/bounds, display scale, timestamps, and source-manifest digest. Reject old PID/DerivedData, production-home resolution, missing argument, wrong owner, occlusion, or predated image.
  - Runtime data: the exact app runs only against the task-local isolated home, so its Core Data, log, and preference writes are disposable and production paths are never opened. Use isolated exact-app state for composition/pointer/Escape checks and a task-local in-memory context plus fixed-clock/fixed-snapshot SwiftUI host for deterministic open/close/category-save updates, empty day, short adjacency, many categories, long CJK, DST, and near-midnight states. Do not change system time or user persistence.
  - Compact scenarios: in the fixed host cover no activity, default category, synthetic open-session growth/category replacement, short adjacent segments, long CJK, and near-midnight rolling range; in the exact app inspect existing data only. Verify `오늘의 흐름`, total, two-hour ticks, patterned block, now marker, visible fallback detail, and no clipping at the measured compact width.
  - Expanded scenarios: use the fixed host for three labelled lanes, past/future distinction, many categories, summary-total equality, and synthetic live category switch; use the exact app only for read-only tasks/timer/settings/notch composition. Verify no horizontal scroll and label which surface proves each criterion.
  - Pointer/keyboard: hover/focus changes detail; leave/blur restores fallback unless pinned; click and Enter/Space pin; accessibility action resets; AX traversal is header→toggle→chronological blocks, while Tab/Shift-Tab keyboard order is toggle→chronological blocks with visible focus. Escape must collapse the exact app panel through `NotchWindow`, not run a Dayline-local reset.
  - Accessibility: use a native accessibility inspection surface or a task-local `AXUIElement` dumper to record role/name/value/action order; use VoiceOver traversal when permission is available. Every segment must expose category/default, range, duration, and active state; decorative shapes stay hidden. If OS permission prevents both inspection paths, mark BLOCKED and report the exact permission instead of claiming pass.
  - Reduce Motion: record prior value, enable it, repeat expansion/selection/live update, and prove no decorative pulse/nonessential transition; restore the prior value. Record and restore Full Keyboard Access and VoiceOver state as well.
  - Acceptance: manual QA matrix covers every scenario with timestamped action/result/evidence; measured required text styles are at least 11pt; a dual visual pass finds no clipping, color-only distinction, CJK tofu, overlapping controls, or stale checkerboard; exact app and fixed host provenance/source digest match Todo 7.
  - QA failure: wrong owner, inaccessible short block, missing fallback detail, total mismatch, category-color discontinuity, stale update, resize clipping, Escape conflict, or settings not restored is FAIL and reopens the owning task.
  - Evidence: `$ATTEMPT_ROOT/task-8-preflight.txt`, `$ATTEMPT_ROOT/task-8-settings-before-after.txt`, `$ATTEMPT_ROOT/task-8-negative-provenance.txt`, `$ATTEMPT_ROOT/task-8-manual-qa.md`, `$ATTEMPT_ROOT/task-8-owner-actions.txt`, `$ATTEMPT_ROOT/task-8-compact-empty.png`, `$ATTEMPT_ROOT/task-8-compact-live.png`, `$ATTEMPT_ROOT/task-8-expanded-live.png`, `$ATTEMPT_ROOT/task-8-keyboard-accessibility.txt`, `$ATTEMPT_ROOT/task-8-reduced-motion.mov`, `$ATTEMPT_ROOT/task-8-visual-pass-a.md`, `$ATTEMPT_ROOT/task-8-visual-pass-b.md`.
  - Cleanup: restore recorded accessibility settings; terminate the exact app with `kill -KILL "$PID"`; verify production SQLite/WAL/SHM, daily logs, app preferences, and login-item state are unchanged; destroy only the in-memory host store; kill host/helpers, remove task scratch/DerivedData, release native-QA lease, and preserve evidence.
  - Adversarial: wrong owner, occlusion, `stale_state`, color-vision dependence, keyboard-only, permission denial, resize extremes, user-data preservation.
  - Commit: N — evidence only.

## Final verification wave

> Runs on frozen source after all todos. F1/F2 may parallelize; F3 and F4 serialize through the native-QA lease, with F4 starting after F3 cleanup. Each writes a fresh report and must return unconditional APPROVE at the same digest. Final lanes never edit; any rejection starts a new attempt and reruns Todos 1–8 plus F1–F4.

- [ ] F1. Plan compliance audit — A `lazycodex-gate-reviewer` checks every Todo 1–8 acceptance item, dependency order, Todo 1 recursive-baseline versus final-tree diff, allowlist/pre-post hashes, evidence freshness, commands/executed counts, cleanup, and retained daily-log/slot contracts. Any non-allowlisted add/change/delete or incomplete owner task reopens its owner. Evidence: `$ATTEMPT_ROOT/F1-plan-compliance.md`. Commit: N.
- [ ] F2. Code quality and security review — A `lazycodex-code-reviewer` reads the complete changed files and tests, then audits Swift concurrency/observer lifetime, one-fetch/cache behavior, deterministic normalization, calendar/DST correctness, value-copy boundary, accessibility semantics, dead production references, project compatibility, persistence safety, and AI-slop/overengineering. Evidence: `$ATTEMPT_ROOT/F2-code-quality-security.md`. Commit: N.
- [ ] F3. Fresh real manual and visual QA — Owned allowlist: `$ATTEMPT_ROOT/F3-*` and `$ATTEMPT_ROOT/native-qa.lock` only. A new `lazycodex-qa-executor` recovers stale native state, acquires the lease, runs `xcodebuild -project NativeScheduler/NativeScheduler.xcodeproj -scheme NativeScheduler -configuration Debug -derivedDataPath "$ATTEMPT_ROOT/F3-DerivedData" build`, records production state, proves a fresh task-local `QA_HOME` routing, and launches the exact executable only with that isolated `HOME`/`CFFIXED_USER_HOME` and volatile login-item argument. It repeats exact-app composition plus fixed-host compact/expanded/synthetic-live-category/pointer/keyboard/Escape/accessibility/Reduce Motion with new PID-bound artifacts; it cannot reuse Todo 8 images or open production persistence. Two visual reviewers pass. It only approves/rejects, terminates with `kill -KILL "$PID"`, verifies unchanged production state, cleans settings/helpers, and releases the lease. Evidence: `$ATTEMPT_ROOT/F3-manual-qa.md` and `$ATTEMPT_ROOT/F3-*`. Commit: N.
- [ ] F4. Scope fidelity and runtime debugging audit — Owned allowlist: `$ATTEMPT_ROOT/F4-*` and `$ATTEMPT_ROOT/native-qa.lock` only. After F3 cleanup, an independent reviewer recovers stale native state, acquires the lease, reproduces the recursive manifest diff, records production state, runs `xcodebuild -project NativeScheduler/NativeScheduler.xcodeproj -scheme NativeScheduler -configuration Debug -derivedDataPath "$ATTEMPT_ROOT/F4-DerivedData" build`, proves a fresh isolated `QA_HOME`, and launches only that exact executable with isolated `HOME`/`CFFIXED_USER_HOME` and volatile login-item argument. It uses the exact app for isolated composition and the accepted-digest in-memory fixed-clock host for save/tick-race plus midnight/DST/overlap hypotheses. It only approves/rejects and proves production state unchanged. On approval it restores guarded settings, cleans the host/helpers, releases the lease, and leaves only this exact verified F4 app running collapsed inside its isolated home for user inspection with PID/path/payload recorded; on rejection it terminates with `kill -KILL "$PID"` and cleans fully. Evidence: `$ATTEMPT_ROOT/F4-scope-runtime-audit.md`. Commit: N.

## Commit strategy

- No commits or staging: the git root is `/Users/chan/Projects`, `native_schedular/` is untracked, and no `HEAD` exists.
- Every implementation task records an explicit allowlist SHA-256 manifest before and after its edit. Verification rejects unexpected changed paths or broad generation/deletion.
- If the user later initializes/authorizes repository history, commits may be reconstructed from accepted task manifests; this execution does not assume that authority.

## Success criteria

- The old checkerboard is absent from `MainPanelView`; `오늘의 흐름` is observable in compact and expanded native surfaces at real measured widths.
- In-memory session-save integration and the fixed host prove start/open, pause/close, and category-replacement records create exact-colored/patterned Dayline blocks and keep the usage summary consistent; read-only exact-app QA proves the surrounding collapsed progress ring/timer composition is preserved without altering user data.
- One fetch occurs per reload; ticks never refetch; relevant saves/day rollover refetch once; a same-day failure preserves the last good snapshot, while a rollover failure shows an empty errored new day and never yesterday's data.
- Normalized segment/category totals match with no overlap double count on normal, 23-hour, and 25-hour days.
- The active/recent detail is always visible; every block has non-color and accessibility meaning; keyboard, Escape ownership, VoiceOver/AX, focus, and Reduce Motion behaviors pass.
- Existing tasks, timer, category/settings, hover/options pinning, notch geometry, collapsed progress ring, `HeatmapSlot`, and daily-log flows remain intact.
- All targeted/full tests and both SwiftPM/Xcode builds pass at the final source digest; Todos 7/8 and F1–F4 have fresh, non-stale artifacts and unconditional approval.
- Only planned allowlist files changed, all temporary resources/settings are cleaned, and the optional final running app is the exact F4-verified collapsed bundle.
