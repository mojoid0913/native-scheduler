# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Icon?
Thumbs.db

# ── Swift Package Manager ─────────────────────────────────────────────────────
# Build artifacts and derived data — machine-local, never commit
.build/
*.o
*.d
*.swp

# SPM resolved versions are intentionally committed so builds are reproducible.
# Package.resolved is NOT listed here.

# ── Xcode ─────────────────────────────────────────────────────────────────────
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcuserstate
*.xcscheme
xcuserdata/
DerivedData/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# ── Project runtime artifacts ─────────────────────────────────────────────────
# Generated by orchestrate.py; not source — should not be committed.
orchestration_output.md

# Manual QA receipts and transient captures
capture-hashes.txt
options-payload-hashes.txt
owner-after-hover.txt
ax-readonly.txt
task-*-*.txt
*.orig
/-
/native_schedular/

# Durable plans and ULW ledgers are retained; generated evidence is not.
/.omo/*
!/.omo/plans/
!/.omo/plans/**
!/.omo/drafts/
!/.omo/drafts/**
!/.omo/ulw-loop/
!/.omo/ulw-loop/**
NativeScheduler/.omo/

# ── Environment / secrets ─────────────────────────────────────────────────────
.env
.env.*
*.pem
*.p12
*.mobileprovision

# ── Editor artifacts ──────────────────────────────────────────────────────────
# Local coding-agent state
.claude/
.serena/

# JetBrains (AppCode, etc.)
.idea/

# VS Code
.vscode/

# Vim / Emacs swap files
*.swp
*~

# Local Python/test/tool caches
.pytest_cache/
.ruff_cache/
env/
tmp/

# ── Logs (application runtime output) ────────────────────────────────────────
# App writes daily logs to ~/Library/Application Support — those are not in the
# repo, so nothing to ignore here. This section is a placeholder comment only.

# ── Misc ──────────────────────────────────────────────────────────────────────
*.log
